# ── Niet van deze repo ──────────────────────────────────────
# Deze map ligt binnen de bredere Eelerwoude GIS-werkruimte en bevat
# daardoor ook MODFLOW-materiaal dat hier niet thuishoort (13 GB).
# Het blijft op schijf staan, maar gaat de repo niet in.
modflow/
CLAUDE-modflow-werkinstructie.md
modflow-pipeline-blokschema.svg
modflow-skillnetwerk-blokschema.svg
files.zip
_claude_memory/
_robocopy_log.txt

# ── Secrets / lokale config ─────────────────────────────────
.env
.env.local
*.pem
*.key

# ── Editor / agent caches ───────────────────────────────────
.cache/
.claude/

# ── Python ──────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.so
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# ── uv / venv (lockfile WEL committen voor reproduceerbaarheid) ──
.venv/
.venv-*/
.venvs/

# ── Jupyter ─────────────────────────────────────────────────
.ipynb_checkpoints/

# ── IDE ─────────────────────────────────────────────────────
.vscode/
.idea/
*.swp

# ── OS ──────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Sessies / caches (per gebruiker, niet delen) ─────────────
sessions/
**/data/cache/
*.tmp
*.bak
