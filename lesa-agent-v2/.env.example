# Kopieer naar .env en vul in — commit .env NOOIT

# Anthropic (verplicht voor agent)
ANTHROPIC_API_KEY=

# PostGIS (optioneel — alleen voor PostgisSessionStore)
POSTGIS_URL=postgresql://ew_pipeline:WACHTWOORD@localhost:5432/ewaarnemingen

# Cache-locatie (optioneel — default: ./data/cache)
GEO_CACHE_DIR=

# Logging
LOG_LEVEL=INFO
